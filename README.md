The code also contains certain modules or packages that are to be imported like tkinter qrcode etc...
you need to import or install them first inorder to run the code 
after you installed and run the code it will prompt you to the window where you can copy paste the link to generate the qrcode
make sure to install the packages first
